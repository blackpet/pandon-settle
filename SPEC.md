# 🎴🪙 판돈정산 (pandon-settle)

> 친구 **2~5명**이 고스톱·포커·훌라를 할 때, **누가 누구에게 얼마를 보내야 하는지**를
> 끝날 때 한 화면에서 알려주는 웹앱.

---

## 0. 이 문서를 읽는 AI/개발자에게

- **최우선 목표는 "입력이 쉬운 것"이다.** 기능을 추가할 때 탭 수가 늘어난다면 다시 생각할 것.
- **두 번째 목표는 "시각적 가독성"이다.** 술 마시며 어두운 방에서 본다는 전제.
- 판돈 계산은 재미로 하는 것이므로, **정확성보다 합의 가능성**이 중요하다.
  룰이 애매하면 하드코딩하지 말고 **설정으로 빼서 사용자가 정하게 한다.**
- 모든 금액 계산은 **정수(원 단위)** 로만 한다. 부동소수점 금지.
- **인원은 2~5명이고 기본은 3명이다.** 계산 코드 어디에도 `3`을 박지 않는다 —
  인원은 언제나 `players.length` 에서 읽는다. 화면도 인원에 따라 늘고 준다.

---

## 1. 기술 스택 / 제약

| 항목 | 결정 |
|---|---|
| 프레임워크 | **없음.** Vanilla JS + ES Modules |
| 빌드 | **없음.** `npx serve` 로 바로 뜨고, 정적 호스팅에 그대로 배포 |
| 상태 저장 | `localStorage` (서버·로그인·계정 전부 없음) |
| 그래프 | 인라인 SVG 직접 그리기 (차트 라이브러리 금지) |
| 대상 | 모바일 세로 화면 우선. 한 사람 폰에서 총무처럼 사용 |
| PWA | `manifest.json` + `sw.js` 로 홈화면 추가 / 오프라인 동작 |
| 테스트 | `node --test` (계산 로직만, DOM 테스트 없음) |

### 파일 구조

```
pandon-settle/
├── SPEC.md              ← 이 문서
├── index.html
├── styles.css
├── manifest.json
├── sw.js
├── src/
│   ├── rules.js         ← 게임별 점수→금액 계산 (순수 함수, 부작용 없음)
│   ├── settle.js        ← 최소 송금 정산 알고리즘 (순수 함수)
│   ├── store.js         ← localStorage 저장/복원/마이그레이션
│   ├── chart.js         ← SVG 누적 그래프
│   └── ui.js            ← 화면 렌더링 · 이벤트
└── test/
    ├── rules.test.js
    └── settle.test.js
```

> `rules.js` 와 `settle.js` 는 **DOM을 절대 참조하지 않는다.** 순수 함수만 export 한다.
> 이 두 파일이 앱의 심장이고, 나머지는 껍데기다.

---

## 2. 데이터 모델

```js
const state = {
  version: 1,

  // 2~5명. id 는 배열 인덱스와 같고, 판이 시작되면 인원을 바꾸지 않는다.
  players: [
    { id: 0, name: "재호", emoji: "🐶", color: "#4DA3FF" },
    { id: 1, name: "철수", emoji: "🐱", color: "#FFB84D" },
    { id: 2, name: "영희", emoji: "🐰", color: "#B47DFF" },
  ],

  game: "gostop",              // "gostop" | "poker" | "hoola"

  pointValue: {                // 게임별로 점당 금액이 다르다 (중요)
    gostop: 100,
    poker:  100,
    hoola:   10,               // 훌라는 잔여점수가 커서 낮게 잡는다
  },

  rules: { /* 3장 참조 */ },

  rounds: [ /* 4장 참조 */ ],

  startedAt: 1755993600000,
}
```

### 저장 규칙

- 키: `pandon-settle:v1`
- **모든 상태 변경 직후 즉시 저장**한다. 앱이 죽어도 한 판도 잃지 않아야 한다.
- 앱 시작 시 저장된 판이 있으면 홈에서 `[이어서 하기 · N판 진행중]` 을 보여준다.
- `version` 이 다르면 마이그레이션 함수를 태운다. 실패하면 **데이터를 지우지 말고**
  백업 키로 옮긴 뒤 새로 시작한다.

---

## 3. 룰 설정 (`state.rules`)

고스톱과 훌라는 지역·모임마다 룰이 크게 다르다.
**하드코딩하지 말고 전부 설정으로 빼되, 기본값만으로도 바로 게임을 시작할 수 있어야 한다.**
설정 화면은 기본적으로 접혀 있는다.

```js
rules: {
  gostop: {
    pibak:        2,      // 피박 배수 (0이면 사용 안 함)
    gwangbak:     2,      // 광박 배수
    meongbak:     2,      // 멍박 배수
    goDouble:     true,   // 3고부터 2배씩 (false면 고당 +1점만)
    shake:        2,      // 흔들기 1회당 배수
    gobak:        true,   // 고박 사용 여부
    scoreCap:     null,   // 점수 상한 (null = 없음). 예: 20
  },
  hoola: {
    dokbak:       2,      // 독박(미등록) 배수
    dokbakMode:   "multiply",  // "multiply" = 잔여점수×2 | "fixed" = 고정점수
    dokbakFixed:  30,     // dokbakMode === "fixed" 일 때 쓰는 점수
    hoolaBonus:   4,      // "훌라"(한 번에 다 냄) 배수
    sevenPoker:   0,      // 7포카드 배수 (0 = 사용 안 함, 켜면 7)
    stopbak:      2,      // 스톱 선언 후 짐 → 스톱박 배수
    ppangBonus:   2,      // 대빵/소빵 배수
  },
  poker: {
    mode: "simple",       // "simple" = 승자+금액, 나머지 반반
                          // "detail" = 개별 입력
    oddTo: "winner",      // simple 모드에서 반반이 안 나눠떨어질 때 나머지 귀속
  },
}
```

---

## 4. 라운드 기록 (`state.rounds`)

```js
// 고스톱
{
  game: "gostop",
  winner: 0,
  score: 7,                                  // 기본 점수
  go: 2,                                     // 고 횟수
  shake: 0,                                  // 흔든 횟수
  gobak: null,                               // 고박이면 해당 플레이어 id
  losers: { 1: { pibak: true }, 2: {} },     // 패자별 개인 배수
  sitout: [],                                // 이번 판 안 친 사람 id 목록 (4~5인)
  deltas: [ 2700, -1800, -900 ],             // ★ 계산 결과 캐시 (길이 = 인원)
  at: 1755993600000,
}

// 포커 (simple)
{ game:"poker", mode:"simple", winner:0, amount:5000, deltas:[5000,-2500,-2500], at:... }

// 포커 (detail)
{ game:"poker", mode:"detail", deltas:[5000,-2000,-3000], at:... }

// 훌라
{
  game: "hoola",
  winner: 0,
  losers: {
    1: { score: 23, dokbak: false, stopbak: false },
    2: { score: 40, dokbak: true,  stopbak: false },
  },
  winnerBonus: { hoola: false, sevenPoker: false, ppang: false },
  deltas: [ 10300, -2300, -8000 ],
  at: ...,
}
```

### ★ `deltas` 캐시가 핵심이다

계산 결과를 **라운드에 박아서 저장**한다. 이유:

1. 나중에 룰 설정을 바꿔도 **이미 끝난 판의 금액이 흔들리지 않는다.**
2. 잔액은 `rounds` 를 매번 접어서 구하므로 별도 상태가 없다 → 상태 불일치가 원천 봉쇄된다.
3. **실행취소가 `rounds.pop()` 한 줄로 끝난다.**

```js
export function balances(rounds, n) {
  const b = new Array(n).fill(0);
  for (const r of rounds) r.deltas.forEach((v, i) => (b[i] += v));
  return b;
}
```

`n` 은 언제나 `state.players.length` 다. 기본값을 두지 않는다 — 인원을 넘기는 걸
빠뜨리면 조용히 3인으로 계산되는 것보다 그 자리에서 터지는 게 낫다.

### 불변식 (invariant)

> **모든 라운드의 `deltas` 합은 반드시 0이고, 길이는 인원과 같다.**

`rules.js` 의 모든 계산 함수는 반환 직전에 이걸 assert 한다.
어긋나면 저장하지 말고 에러를 던진다. 테스트에서도 매 케이스마다 검증한다.

---

## 5. 게임별 계산 규칙

### 5-1. 🎴 고스톱

승자 1명이 **나머지 참여자 각각에게** 점수만큼 받는다. 3인이면 두 명에게,
5인이면 네 명에게 받는다 — 인원이 늘면 승자가 버는 돈도 그만큼 커진다.

```
공통점수 = (기본점수 + 고횟수) × 고배수 × 흔들기배수

  고배수     = (goDouble && go >= 3) ? 2^(go-2) : 1
               → 3고 ×2, 4고 ×4, 5고 ×8
  흔들기배수 = shake^흔든횟수   (기본 2^n)

각 패자 지불 = 공통점수 × 개인배수 × 점당금액

  개인배수 = (피박 ? 2 : 1) × (광박 ? 2 : 1) × (멍박 ? 2 : 1)

승자 획득 = 모든 패자 지불액의 합
빠진 사람 = 0원 (아래 참조)
```

**개인 배수 조건** (UI 툴팁으로 노출할 것)

| 배수 | 조건 | 적용 대상 |
|---|---|---|
| 피박 | 승자가 피로 점수를 냈는데 그 패자의 피가 5장 이하 | 해당 패자만 |
| 광박 | 승자가 광으로 점수를 냈는데 그 패자가 광을 하나도 못 먹음 | 해당 패자만 |
| 멍박 | 승자가 열끗 7장 이상인데 그 패자가 열끗을 하나도 못 먹음 | 해당 패자만 |

> 멍박을 "두 패자 모두"로 치는 모임도 있다. 그런 경우엔 칩을 두 명 다 누르면 되므로
> **UI는 피박·광박과 똑같이 패자별 칩으로 통일한다.**

**고박** — 고를 외쳤다가 상대가 나버린 경우. 고 외친 사람이 전부 뒤집어쓴다.

```
고박자 지불 = 공통점수 × 2 × 고박자개인배수 × 점당금액
나머지 패자 = 0원
승자       = 고박자가 낸 금액 전부
```

**빠짐 (4~5인)** — 전통 룰에서 4인이면 한 명이 광을 팔고 쉰다.
`sitout` 에 들어간 사람은 이 판에서 **0원**이고, 승자가 받는 금액에도 끼지 않는다.
기본은 전원 참여이므로 `sitout` 은 보통 빈 배열이다 —
평범한 판의 탭 수는 인원이 몇이든 늘지 않는다.

`scoreCap` 이 설정되어 있으면 **공통점수 산출 직후** 상한을 적용한다 (개인 배수 적용 전).

### 5-2. ♠️ 포커

- **simple** — 승자와 딴 금액만 입력. 나머지 `k` 명이 `amount/k` 씩 부담한다
  (3인이면 둘이 반씩, 5인이면 넷이 1/4씩).
  나눠떨어지지 않으면 `oddTo` 설정에 따라 1원 단위 나머지를 귀속시킨다.
  기본(`"winner"`)은 패자가 전원 `floor(amount/k)` 만 내고 **승자가 나머지를 덜 받는다** —
  잃은 사람 부담이 설정 때문에 늘어나는 일이 없게 한다.
  `"loser"` 면 앞쪽 패자부터 1원씩 더 내고 승자가 `amount` 를 온전히 받는다.
- **detail** — `n` 명 중 **n−1명만 입력받고 마지막 한 명은 자동 계산**한다.
  자유 입력 칸이 하나 모자라므로 합계가 0이 아닐 수가 없다. 검증 에러 자체를 없앤 설계다.

### 5-3. 🃏 훌라

먼저 다 내려놓거나 스톱에서 이긴 사람이 승자. 패자는 **손에 남은 카드 점수**만큼 낸다.
인원이 몇이든 계산은 같다 — 패자가 늘면 승자가 받는 줄이 늘어날 뿐이다.

```
카드 점수: A=1, 2~10=숫자 그대로, J=11, Q=12, K=13

승자보너스 = (훌라 ? 4 : 1) × (7포카드 ? 7 : 1) × (대빵/소빵 ? 2 : 1)

각 패자 지불 = 유효점수 × 개인배수 × 승자보너스 × 점당금액

  유효점수 = 독박 && dokbakMode==="fixed" ? dokbakFixed : 잔여점수
  개인배수 = (독박 ? 2 : 1) × (스톱박 ? 2 : 1)
```

| 용어 | 뜻 |
|---|---|
| 독박 | 한 장도 등록 못 하고 7장 그대로 들고 끝남 |
| 스톱박 | 스톱을 외쳤는데 더 낮은 점수의 사람이 있어서 오히려 짐 |
| 훌라 | 한 번에 전부 내려놓고 남 |
| 대빵/소빵 | 등록 없이 합 83점 이상 / 15점 이하인 특수 족보 |
| 7포카드 | 숫자 7 카드 4장 |

> 훌라는 잔여 점수가 20~80점까지 나와서 고스톱 점수와 자릿수가 다르다.
> **반드시 점당 금액을 게임별로 분리해서 쓸 것** (`pointValue.hoola` 기본 10원).

---

## 6. 정산 알고리즘 (`settle.js`)

잔액 합은 항상 0이다. 그리디 매칭은 한 번 돌 때마다 최소 한 사람을 소진시키므로
**송금은 `인원 − 1` 건을 넘지 않는다.** 3인이면 2건, 5인이면 4건이다.

**3인 이하에서는 이게 곧 최소 건수이기도 하다** (2인은 항상 1건, 3인은 2건).
4인 이상에서는 잔액이 우연히 딱 맞아떨어지는 조합을 찾으면 더 줄일 수 있지만,
그 최적화는 하지 않는다 — 판돈 계산은 **정확성보다 합의 가능성**이고,
"큰 사람부터 큰 사람에게"가 사람이 납득하기 가장 쉬운 순서다.

```js
export function settle(balances) {
  const plus  = balances.map((v,i)=>({i,v})).filter(x=>x.v>0).sort((a,b)=>b.v-a.v);
  const minus = balances.map((v,i)=>({i,v})).filter(x=>x.v<0).sort((a,b)=>a.v-b.v);
  const out = [];
  let p = 0, m = 0;
  while (p < plus.length && m < minus.length) {
    const amt = Math.min(plus[p].v, -minus[m].v);
    out.push({ from: minus[m].i, to: plus[p].i, amount: amt });
    plus[p].v -= amt;  minus[m].v += amt;
    if (plus[p].v === 0) p++;
    if (minus[m].v === 0) m++;
  }
  return out;   // length <= 인원 - 1 보장
}
```

---

## 7. 화면 명세

### 7-1. 홈

```
        🎴🪙
    판 돈 정 산
  "동전은 없어도 계산은 정확하게"

  [  새 판 시작  ]
  [ 이어서 하기 · 8판 진행중 ]
```

### 7-2. 세팅

- **인원 선택 `[2] [3] [4] [5]`** — 기본 3명. 누르면 아래 이름 칸이 늘고 준다
  (줄일 때 지워지는 이름은 다시 늘리면 되살아난다)
- 플레이어 이름 입력 (아바타 이모지·색상 자동 배정, 탭해서 변경 가능)
- 게임 선택: 🎴 고스톱 / ♠️ 포커 / 🃏 훌라
- 점당 금액: `100 / 500 / 1000` 프리셋 + 직접 입력
- `▸ 우리 룰 설정` (접힌 상태. 안 열어도 게임 시작 가능)

### 7-3. 메인 — 고스톱

```
┌──────────────────────────────┐
│  8판째 · 고스톱 · 점당 100원   │
├──────────────────────────────┤
│  🐶 재호      +3,200   🤑     │
│  🐱 철수      -1,500   😥     │
│  🐰 영희      -1,700   🫠     │
├──────────────────────────────┤
│  누가 났어?                   │
│  [🐶재호] [🐱철수] [🐰영희]    │  ← 탭 1
├──────────────────────────────┤
│  몇 점?   ▸ 7                 │  ← 탭 2 (숫자패드)
│   1 2 3                       │
│   4 5 6                       │
│   7 8 9                       │
│   ← 0 ✓                      │
├──────────────────────────────┤
│  [고박]                       │  ← 예외 상황 (거의 안 씀)
│  🐶재호 [1고][2고][3고][흔들기] │  ← 해당될 때만
│  🐱철수 [피박][광박][멍박]      │
│  🐰영희 [피박][광박][멍박]      │
│  (4~5인이면 각 줄 끝에 [빠짐])  │
├──────────────────────────────┤
│  ┌ 미리보기 ────────────┐     │  ← 칩 누를 때마다 실시간
│  │ 재호 +2,700          │     │
│  │ 철수 -1,800  💀      │     │
│  │ 영희   -900          │     │
│  └─────────────────────┘     │
│        [  확  인  ]           │
├──────────────────────────────┤
│  ↩ 실행취소      📋 판 기록    │
└──────────────────────────────┘
```

**핵심 원칙: 배수 칩은 기본 전부 꺼져 있다.** 평범한 판은 `승자 탭 → 점수 탭 → 확인` 으로 끝난다.
**인원이 늘어도 이 세 번은 그대로다.** 잔액 줄과 배수 칩 줄만 늘어난다.

### 7-4. 메인 — 훌라

```
누가 났어?   [🐶재호]

🐱 철수  남은점수 [ 23 ]   [독박] [스톱박]
🐰 영희  남은점수 [ 40 ]   [독박]✓[스톱박]
🐶 재호  보너스   [훌라 ×4] [7포카드 ×7] [대빵/소빵]

  → 미리보기 → 확인
```

### 7-5. 정산

```
┌────────────────────────────┐
│   오늘의 결과 · 총 24판       │
│                            │
│   🐶 재호   +12,400   🤑     │
│   🐱 철수    +3,100   😏     │
│   🐰 영희   -15,500   🫠     │
│           👑 오늘의 호구      │
├────────────────────────────┤
│   💸 이렇게 보내면 끝          │
│   영희 → 재호   12,400원     │
│   영희 → 철수    3,100원     │
├────────────────────────────┤
│   📈 누적 그래프              │
│   [3색 꺾은선 · 0선 강조]     │
├────────────────────────────┤
│   🏆 오늘의 기록              │
│   최고 한 판   재호 4,000원   │
│   승률        재호 45%       │
│   피박 최다    영희 6회 💀    │
├────────────────────────────┤
│   [ 텍스트로 공유 ]           │
└────────────────────────────┘
```

**공유 텍스트 포맷** (`navigator.share` / 클립보드 복사)

```
🎴🪙 판돈정산 · 8/24 · 고스톱 24판

🤑 재호  +12,400
😏 철수   +3,100
🫠 영희  -15,500

💸 정산
영희 → 재호  12,400원
영희 → 철수   3,100원
```

---

## 8. 익살 요소 — 감정 이모지

잔액에 따라 자동으로 붙는다.

| 상태 | 이모지 | 조건 |
|---|---|---|
| 1등 | 🤑 | 잔액 최대 & 플러스 |
| 소폭 이득 | 😏 | 플러스인데 1등 아님 |
| 본전 | 😐 | 정확히 0 |
| 소폭 손실 | 😥 | 마이너스인데 꼴찌 아님 |
| 꼴찌 | 🫠 + `👑 오늘의 호구` | 잔액 최소 |

동점이면 해당되는 사람 모두에게 붙는다 (1등이 둘이면 둘 다 🤑).
전원이 본전이면 아무도 1등도 꼴찌도 아니다 — 전부 😐 다.
| 직전 판에 배수 맞음 | 💀 | 마지막 라운드에서 피박/광박/독박 당함 |

판 입력 직후 토스트 1.5초: **`철수 → 재호 1,800원 💀 피박`**

---

## 9. 디자인 토큰

```css
:root {
  --bg:        #12131A;   /* 밤에 게임하니까 다크 고정 */
  --surface:   #1C1E28;
  --border:    #2A2D3A;
  --text:      #ECEDF2;
  --text-dim:  #8B8FA3;

  --win:       #3DDC84;   /* 플러스 */
  --lose:      #FF6B6B;   /* 마이너스 */
  --even:      #8B8FA3;   /* 본전 */
  --accent:    #FFD54D;   /* 강조 (동전) */

  /* 플레이어 고정색 — 5명까지. 승패색(--win/--lose)과 겹치지 않게 고른 색상각이다 */
  --p1: #4DA3FF;  --p2: #FFB84D;  --p3: #B47DFF;
  --p4: #35D6C4;  --p5: #FF7EC7;

  --tap-min:   56px;      /* 터치 타겟 최소 크기 */
  --radius:    14px;
}
```

- 금액은 **`font-variant-numeric: tabular-nums`** 로 자릿수 고정 → 갱신 시 안 흔들림
- 플레이어 색은 잔액 카드·배수 칩·그래프 선에서 **동일하게** 쓴다
- 잔액 변경 시 숫자 롤링 애니메이션 (300ms, `prefers-reduced-motion` 존중)
- 배수 칩은 꺼짐 = 테두리만 / 켜짐 = 채워짐 + 살짝 확대

---

## 10. 구현 순서

1. `settle.js` + 테스트 — 정산 알고리즘부터. 가장 중요하고 가장 짧다
2. `rules.js` + 테스트 — 게임 3종 계산. **UI 없이 테스트만으로 완성**시킬 것.
   처음부터 인원을 `deltas.length` 로 다룬다. 3인으로 짜고 나중에 여는 건 두 배로 든다
3. `store.js` — localStorage 저장/복원
4. `index.html` + `ui.js` — 고스톱 입력 화면만 (포커·훌라는 나중)
5. 정산 화면 + 공유 텍스트
6. 포커 · 훌라 입력 화면
7. `chart.js` — SVG 그래프
8. 통계 · 기록 화면
9. 룰 설정 화면
10. PWA (`manifest.json`, `sw.js`)

> 4번까지 되면 **이미 실전에서 쓸 수 있다.** 거기서 한 번 실제로 써보고 나머지를 정하는 걸 권장.

---

## 11. 테스트 케이스 (검증 완료된 기대값)

### 고스톱 — 3인 · 점당 100원

| 상황 | 기대 `deltas` |
|---|---|
| 7점, 2고, 철수 피박 | `[+2700, -1800, -900]` |
| 7점, 3고, 배수 없음 | `[+4000, -2000, -2000]` |
| 5점, 철수 피박+광박 | `[+2500, -2000, -500]` |
| 4점, 흔들기 1회, 영희 광박 | `[+2400, -800, -1600]` |

계산 예시 (1번): `(7 + 2고) = 9점` → 철수 `9 × 2(피박) × 100 = 1,800` / 영희 `9 × 100 = 900` / 재호 `+2,700`

### 인원을 바꿨을 때 — 점당 100원 (훌라만 10원)

| 상황 | 기대 `deltas` |
|---|---|
| 2인 고스톱 3점 | `[+300, -300]` |
| 4인 고스톱 5점 | `[+1500, -500, -500, -500]` |
| 5인 고스톱 5점, 5번이 빠짐 | `[+1500, -500, -500, -500, 0]` |
| 4인 포커 simple 5,000원 (`oddTo="winner"`) | `[+4998, -1666, -1666, -1666]` |
| 4인 포커 simple 5,000원 (`oddTo="loser"`) | `[+5000, -1667, -1667, -1666]` |
| 4인 훌라 잔여 10·20·30 (점당 10원) | `[+600, -100, -200, -300]` |

5,000원을 셋이 나누면 1,666원씩에 2원이 남는다. 기본값에서는 **승자가 4,998원만 받고**
패자 부담은 1,666원에서 늘지 않는다.

### 정산 — 인원별

| 잔액 | 기대 송금 |
|---|---|
| `[+3000, -3000]` | 2번→1번 3,000 **(1건)** |
| `[+5000, +2000, -1000, -2000, -4000]` | 5번→1번 4,000 / 4번→1번 1,000 / 4번→2번 1,000 / 3번→2번 1,000 **(4건 = 인원−1)** |

### 정산

| 잔액 | 기대 송금 |
|---|---|
| `[+400, +1150, -1550]` | 영희→철수 1,150 / 영희→재호 400 |
| `[+3000, -3000, 0]` | 철수→재호 3,000 (1건) |
| `[+2500, +1500, -4000]` | 영희→재호 2,500 / 영희→철수 1,500 |
| `[0, 0, 0]` | 없음 |

### 불변식 테스트 (모든 케이스에 공통 적용)

- `deltas.reduce((a,b)=>a+b, 0) === 0`
- `deltas.length === players.length`
- 모든 `deltas` 원소가 정수
- 정산 결과 `settle().length <= 인원 - 1`
- `settle()` 결과의 송금액 합이 플러스 잔액 총합과 일치
- 인원 2·3·4·5 **전부**에 대해 위를 돌린다 — 3인만 통과하는 코드가 나오지 않게

---

## 12. 아직 안 정한 것 (구현 전 확인 필요)

1. **포커를 실제로 어떻게 하는가?**
   칩을 쓴다면 판마다 입력할 게 아니라 **"끝날 때 최종 칩 개수만 입력"** 하는 방식이
   압도적으로 편하다. 이 경우 포커는 라운드 기록이 아예 필요 없고 화면이 완전히 달라진다.
   → 확정 전까지는 `simple` 모드만 구현하고 넘어간다.

2. **고박 시 개인 배수를 어떻게 볼 것인가** — 현재는 고박자 본인 배수만 적용하도록 정의.
   실제로 쳐보고 어색하면 조정.

3. **게임 도중 게임 종류 변경** — 고스톱 치다가 훌라로 넘어가는 경우.
   현재 설계는 라운드마다 `game` 필드가 있으므로 기술적으로는 가능.
   UI에서 허용할지만 결정하면 된다. (허용을 권장)

4. **판 도중 인원 변경** — 늦게 온 친구가 끼거나 먼저 가는 경우.
   `deltas` 길이가 라운드마다 달라지므로 `balances()` 가 짧은 배열을 만나면 뒤를 0으로 채워야 한다.
   지금은 **판이 시작되면 인원 고정**이고, 한 명이 잠깐 쉬는 건 고스톱의 `sitout` 으로만 표현한다.
   실제로 필요해지면 그때 연다.

5. **5인 고스톱을 정말 그렇게 치는가.** 전통 룰이라면 4인부터 한 명이 광을 팔고 쉬므로
   5인은 두 명이 쉬는 셈이다. `sitout` 이 여러 명을 받으므로 표현은 되지만,
   실제로 그렇게 치는 모임이 있는지는 확인 안 됐다. 쳐보고 어색하면 조정.

---

## 13. 하지 말 것

- ❌ 서버·로그인·회원가입 — 이 앱의 존재 이유가 사라진다
- ❌ 차트 라이브러리 도입 — 꺾은선 3개에 번들 200KB는 과하다
- ❌ 계산 결과를 잔액 변수에 누적 저장 — 반드시 `rounds` 에서 매번 계산
- ❌ 배수 규칙 하드코딩 — 전부 `rules` 설정을 거쳐야 한다
- ❌ 입력 단계 추가 — 새 기능이 탭 수를 늘린다면 기본값을 다시 설계할 것
